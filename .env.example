# Server-side only — never expose these to the client.
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional — question logging silently skips if unset.
# Get a key at https://posthog.com (project settings).
POSTHOG_API_KEY=
# Defaults to https://us.i.posthog.com — change if your project is in the EU.
POSTHOG_HOST=
